# Optional cloud database connection. Local browser mode works without it.
DATABASE_URL=

# Auth.js GitHub OAuth. Set a high-entropy secret and both provider values to enable account login.
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Required by the settlement endpoint when Vercel Cron is enabled.
CRON_SECRET=
